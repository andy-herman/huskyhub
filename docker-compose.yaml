services:

  huskyhub-nginx:
    image: nginx:latest
    container_name: huskyhub-nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
      - ./flask/app/static:/var/www/html/static
    depends_on:
      - huskyhub-flask

  huskyhub-flask:
    build:
      context: .
      dockerfile: ./flask/Dockerfile
    container_name: huskyhub-flask
    environment:
      DB_HOST: huskyhub-db
      DB_USER: ${MYSQL_USER}
      DB_PASSWORD: ${MYSQL_PASSWORD}
      DB_NAME: ${MYSQL_DATABASE}
      FLASK_ENV: development
      FLASK_DEBUG: 1
      FLASK_SECRET_KEY: ${FLASK_SECRET_KEY:-dev-secret-huskyhub-2024}
    volumes:
      - ./flask/app/uploads:/app/app/uploads
    depends_on:
      huskyhub-db:
        condition: service_healthy
    extra_hosts:
      - host.docker.internal:host-gateway

  huskyhub-db:
    image: mysql:8.0
    container_name: huskyhub-db
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_PASSWORD}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
    volumes:
      - mysqldata:/var/lib/mysql
      - ./database/init.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "${MYSQL_PORT:-3306}:3306"
    restart: always
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  huskyhub-ollama:
    image: ollama/ollama:latest
    container_name: huskyhub-ollama
    profiles:
      - ai
    ports:
      - "11434:11434"
    volumes:
      - ollamadata:/root/.ollama

volumes:
  mysqldata:
  ollamadata:
